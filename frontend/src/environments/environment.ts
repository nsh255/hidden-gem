export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000'  // Make sure this matches your backend URL without trailing slash
  // If your backend is running with a different port or host, update this value
};
